// Tariq
//
// This progarm  will determine the number of months it will take to completely pay off debt.

public class U4A4
{
	public U4A4(double x, y, z)
	{
		double bal = x;
		if((y/100)*bal > 20) pay = (y/100)*bal;
		else pay = 20;
		double pay = (y/100) * bal;
		double apr = z;
	}
	
	public double getMonths()
	{
		double min
	}
}